# Окружение курса «Введение в компьютерное зрение», ИУ, Fall 2026.
# Фиксируется на неделе 0 и не меняется в течение семестра (WP0 §8.1).
#
# Библиотека курса cvcourse ставится как пакет (editable, из этого каталога)
# вместе со своими зависимостями — версии закреплены в pyproject.toml,
# в том числе opencv-contrib-python==4.14.0.94. После установки cvcourse
# импортируется из любого каталога, и ноутбуки занятий запускаются без PYTHONPATH.
#
# Команду выполнять из корня репозитория:  pip install -r requirements.txt
-e .

# инструменты: ноутбуки и самопроверка
jupyterlab>=4.0
ipywidgets>=8.1
pytest>=8.0
